# ui_issues

## ui_issues_to_fix_v1
- slightly reduce text size to allow for better ui fit
- slight padding/space to the right of the text "build_artifacts_found"
-"system_health" is not actually outputting any real data
- ensure all text is accurately reflecting real-time outputs, no fake claims, for example is appears we are constantly stating "LOAD: 12%" only output real actual values or do not include the text strings at all
-"activity_log" should show the full 
-"artifact_inventory" reduce text size, ensure all columns align vertically, make rows collapsible so i can read full "component_path" if necessary
-we can reduce size of "purge_parameter_v2"
-"artifact_inventory" needs its own dedicated vertical scroll bar adhering with our current application design aesthetic
-add a new "history" section to our sidebar, where users can review the history of past cleaning runs, they should be able to review the paths deleted per respective run
-"select_scan_root" also needs a dedicated vertical scrollbar adhering with our application aesthetic and styling
