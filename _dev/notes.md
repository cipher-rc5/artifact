# notes

```bash
cargo clean && cargo run --release
```


❯ we need to reduce the height of our back/forward buttons on the "select_scan_root" section the buttons are currently way too large, on our "purge_parameter_v2"
   i also want less vertical height and some spacing from the top of the container. in our "artifact_inventory" after results are returned we also should have
  the ability to select all to clear all. I also want to ensure our application is multi-threaded, because currently when deleting instances our application
  locks up and i get the mac os loading beach ball spinner(i dont want to get the beach ball spinner)
